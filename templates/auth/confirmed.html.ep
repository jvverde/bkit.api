<!DOCTYPE html>
<html>
  <head><title><%= $username %></title></head>
  <style type="text/css">
    html, body{
      width: 100%;
      height: 100%;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items:center;
      flex-flow: column;
    }
  </style>
  <body>
    <div>
      You are now registered as <i><%= $username %></i>
    </div>
    <div>
      Please wait until the administrator approve your account.
    </div>
  </body>
</html>